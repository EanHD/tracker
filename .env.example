# Tracker Configuration File
# This file contains your personal configuration settings.
#
# IMPORTANT: 
# - For installed usage, edit: ~/.config/tracker/.env
# - For development, create .env in project root
#   (takes precedence over ~/.config/tracker/.env)
# - Never commit actual credentials to git!

# Encryption Key (required)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# AI Provider Configuration
# Choose one provider and configure its settings below

# Option 1: Anthropic Claude (recommended for quality)
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-3-5-haiku-20241022  # or claude-3-5-sonnet-20241022

# Option 2: OpenAI GPT
# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4  # or gpt-4-turbo, gpt-3.5-turbo

# Option 3: OpenRouter (access to many models)
# AI_PROVIDER=openrouter
# OPENROUTER_API_KEY=sk-or-your-key-here
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Option 4: Local AI (Ollama) - free, private, offline
# AI_PROVIDER=local
# LOCAL_API_URL=http://localhost:11434/v1
# LOCAL_MODEL=llama3.2:3b  # or granite3-dense:2b, gemma2:2b

# API Server Configuration
API_HOST=localhost
API_PORT=5703
JWT_SECRET=your-jwt-secret-here  # Generate with: openssl rand -hex 32
JWT_EXPIRY_DAYS=90

# Database Configuration
DATABASE_URL=sqlite:///~/.config/tracker/tracker.db


# API Server
API_HOST=localhost
API_PORT=5703
JWT_SECRET=0dcc9c92c419f6bee1f3395a647bc77c677cb1bef425e5229a0d79a91b6ff9ac
JWT_EXPIRY_DAYS=90

# Database
DATABASE_URL=sqlite:///~/.config/tracker/tracker.db



# AI Provider Configuration
AI_PROVIDER=local
LOCAL_API_URL=http://localhost:11434/v1

